server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Microservice-practice-trymad/configs.git
          clone-on-start: true
          search-paths:
          - '{application}'
